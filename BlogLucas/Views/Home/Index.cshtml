@model List<Postagem>
@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1 class="display-4">Blog Lucas&Davi</h1>
    <p>Confira aqui as últimas novidades e notícias do mundo!!!</p>
</div>

@foreach (Postagem postagem in Model) {
    <img src="@postagem.Foto">
    <p>@postagem.Nome</p>
    <p>@Html.Raw(postagem.Texto)</p>
}